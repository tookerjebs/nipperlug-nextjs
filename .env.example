# GitHub Personal Access Token for Gist sharing (server-side only)
# Required for the Build Planner share functionality
# Create a token at: https://github.com/settings/tokens
# Required scopes: gist
GITHUB_TOKEN=your_github_token_here